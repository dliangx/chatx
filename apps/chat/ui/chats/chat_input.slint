export component ChatInputView inherits Rectangle {
    in property <string> placeholderText;
    in property <bool> isSendButtonEnabled;
    in property <bool> isRecording;
    in property <bool> isRecordingPaused;
}
