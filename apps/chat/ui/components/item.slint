import { Icon, Glyph, Variant } from "./icon.slint";
import { Theme } from "../theme.slint";
import { AppState } from "./app_state.slint";

export component ChatListItem inherits Rectangle {
    in property <int> id;
    in property <image> image;
    in property <string> title;
    in property <string> time;
    in property <string> content;
    in property <bool> selected: false;
    callback clicked(int);

    background: (!AppState.is-mobile && (ta.pressed || root.selected)) ? Theme.brand : transparent;

    HorizontalLayout {
        width: 100%;
        height: 100%;
        padding-left: 8px;
        padding-right: 8px;
        spacing: 10px;

        Image {
            width: 40px;
            height: 40px;
            source: root.image;
        }

        VerticalLayout {
            horizontal-stretch: 1;
            spacing: 8px;

            Text {
                width: 100%;
                height: 24px;
                text: root.title;
                font-size: 14px;
                font-weight: 800;
                color: (!AppState.is-mobile && (ta.pressed || root.selected)) ? Theme.brand-soft : Theme.fg;
                overflow: elide;
                vertical-alignment: bottom;
            }

            Text {
                width: 100%;
                height: 24px;
                text: root.content;
                font-size: 12px;
                color: (!AppState.is-mobile && (ta.pressed || root.selected)) ? Theme.brand-soft : Theme.fg-muted;
                overflow: elide;
                vertical-alignment: top;
            }
        }

        Text {
            width: 80px;
            text: root.time;
            font-size: 12px;
            horizontal-alignment: right;
            vertical-alignment: center;
            color: (!AppState.is-mobile && (ta.pressed || root.selected)) ? Theme.brand-soft : Theme.fg-muted;
        }
    }

    Rectangle {
        x: 56px;
        y: root.height;
        width: 100%;
        height: 1px;
        background: Theme.border;
    }

    ta := TouchArea {
        width: 100%;
        height: 100%;
        clicked => { root.clicked(root.id); }
    }
}

export component ContactListItem inherits Rectangle {
    in property <int> id;
    in property <image> image;
    in property <string> peer_id;
    in property <string> title;
    in property <bool> selected: false;
    callback clicked();

    background: (!AppState.is-mobile && (ta.pressed || root.selected)) ? Theme.brand : transparent;

    HorizontalLayout {
        width: 100%;
        height: 100%;
        padding-left: 8px;
        padding-right: 8px;
        spacing: 10px;

        Image {
            width: 40px;
            height: 40px;
            source: root.image;
        }

        Text {
            horizontal-stretch: 1;
            text: root.title;
            font-size: 14px;
            font-weight: 800;
            color: (!AppState.is-mobile && (ta.pressed || root.selected)) ? Theme.brand-soft : Theme.fg;
            vertical-alignment: center;
        }
    }

    ta := TouchArea {
        width: 100%;
        height: 100%;
        clicked => { root.clicked(); }
    }
}

export component SettingItem inherits Rectangle {
    in property <int> id;
    in property <Glyph> icon;
    in property <string> title;
    in property <bool> selected: false;
    callback clicked(int);
    height: 48px;
    background: (!AppState.is-mobile && (ta.pressed || root.selected)) ? Theme.brand : transparent;

    Icon {
        x: 8px;
        y: (parent.height - self.size) / 2;
        size: 24px;
        glyph: root.icon;
        variant: Variant.raw;
        tint: (!AppState.is-mobile && (ta.pressed || root.selected)) ? Theme.brand-soft : Theme.fg;
    }

    Text {
        x: 40px;
        y: 0px;
        width: parent.width - 32px;
        height: parent.height;
        vertical-alignment: center;
        text: root.title;
        font-size: 14px;
        color: (!AppState.is-mobile && (ta.pressed || root.selected)) ? Theme.brand-soft : Theme.fg;
    }

    Icon {
        x: parent.width - 24px;
        y: (parent.height - self.size) / 2;
        size: 20px;
        glyph: Glyph.right;
        variant: Variant.raw;
        tint: (!AppState.is-mobile && (ta.pressed || root.selected)) ? Theme.brand-soft : Theme.fg;
    }

    Rectangle {
        x: 40px;
        y: 48px;
        width: 100%;
        height: 1px;
        background: #ececec;
    }

    ta := TouchArea {
        width: 100%;
        height: 100%;
        clicked => { root.clicked(root.id); }
    }
}

export component PlantItem inherits Rectangle {
    in property <string> key;
    in property <string> value;
    height: 48px;

    Text {
        x: 0px;
        y: 0px;
        width: parent.width / 4;
        height: parent.height;
        text: root.key;
        font-size: 14px;
        color: Theme.fg;
        vertical-alignment: center;
    }

    Text {
        x: parent.width / 4;
        y: 0px;
        width: parent.width * 3 / 4;
        height: parent.height;
        text: root.value;
        font-size: 14px;
        color: Theme.fg;
        vertical-alignment: center;
    }
}
